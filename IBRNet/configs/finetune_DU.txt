# configs/finetune_DU.txt
# IBRNet + DU (transforms.json) 실험용 설정

# 어떤 데이터셋을 쓸지 (dataset_dict 키)
train_dataset = DU

# 여러 데이터셋 섞어 쓸 때 가중치인데, 지금은 하나만 쓰니까 1.0
dataset_weights = 1.0

# 우리 데이터 루트: C:/NovelView_IBRNet/data/classrooms/609 이런 구조
rootdir = C:/NovelView_IBRNet/data/classrooms

# 학습에 사용할 씬 이름들 (폴더 이름 기준)
train_scenes = 609

# --------- 학습 관련 하이퍼파라미터 ---------

# 한 iteration당 샘플링할 ray 개수 (공식 코드 기본값도 이 근처)
N_rand = 1024

# 총 iteration 수 (= max_steps 비슷한 역할)
n_iters = 10000

# coarse / fine 샘플 개수
N_samples = 64
N_importance = 64

# 학습률 (필요하면 나중에 조정)
lrate_feature = 5e-4
lrate_mlp = 5e-4

# learning rate decay 관련 (그냥 기본 느낌으로)
lrate_decay_factor = 0.5
lrate_decay_steps = 200000

# coarse / fine feature 차원 (원래 config 참고해서)
coarse_feat_dim = 32
fine_feat_dim = 32

# 한 타겟 뷰당 선택할 소스 뷰 수 (DU dataset 구현과 맞추기)
num_source_views = 4

# 기타 옵션들은 기본값 사용 (필요하면 나중에 켜기)
# inv_uniform = False
# det = False
# white_bkgd = False

# 몇 iteration마다 로그/이미지/weight 저장할지
i_print   = 100
i_img     = 1000
i_weights = 5000

# 실험 이름 (결과가 logs/expname_* 아래 저장됨)
expname = DU_609

# configs/finetune_DU.txt
# IBRNet + DU (transforms.json) 실험용 설정

# 어떤 데이터셋을 쓸지 (dataset_dict 키)
train_dataset = DU

# 여러 데이터셋 섞어 쓸 때 가중치인데, 지금은 하나만 쓰니까 1.0
dataset_weights = 1.0

# 우리 데이터 루트: C:/NovelView_IBRNet/data/classrooms/609 이런 구조
rootdir = C:/NovelView_IBRNet/data/classrooms

# 학습에 사용할 씬 이름들 (폴더 이름 기준)
train_scenes = 609

# --------- 학습 관련 하이퍼파라미터 ---------

# 한 iteration당 샘플링할 ray 개수 (공식 코드 기본값도 이 근처)
N_rand = 1024

# 총 iteration 수 (= max_steps 비슷한 역할)
n_iters = 10000

# coarse / fine 샘플 개수
N_samples = 64
N_importance = 64

# 학습률 (필요하면 나중에 조정)
lrate_feature = 5e-4
lrate_mlp = 5e-4

# learning rate decay 관련 (그냥 기본 느낌으로)
lrate_decay_factor = 0.5
lrate_decay_steps = 200000

# coarse / fine feature 차원 (원래 config 참고해서)
coarse_feat_dim = 32
fine_feat_dim = 32

# 한 타겟 뷰당 선택할 소스 뷰 수 (DU dataset 구현과 맞추기)
num_source_views = 4

# 기타 옵션들은 기본값 사용 (필요하면 나중에 켜기)
# inv_uniform = False
# det = False
# white_bkgd = False

# 몇 iteration마다 로그/이미지/weight 저장할지
i_print   = 100
i_img     = 1000
i_weights = 5000

# 실험 이름 (결과가 logs/expname_* 아래 저장됨)
expname = DU_609

eval_dataset = DU
eval_scenes = 609
